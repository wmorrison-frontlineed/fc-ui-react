export const ssIcon = (icon) => {
    return icon ? `fa-${icon}-before` : null;
}
export const ssIconBefore = (icon) => {
    return icon ? `fa-${icon}-before` : null;
}
export const ssIconAfter = (icon) => {
    return icon ? `fa-${icon}-after` : null;
}
